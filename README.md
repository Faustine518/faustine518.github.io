# faustine

Personal writing archive for faustine.

## 添加文章

1. 复制 `posts/first-note.html` 并重命名。
2. 修改标题、日期、分类和正文。
3. 在 `archive.html` 添加文章条目，并在 `index.html` 更新最近文章。

The site is plain HTML and CSS, with no build step or external dependencies.